# Timetable axis is 00:00–24:00; blackout is Closed

The member Timetable always shows the full calendar day. Hours outside a room's template window stay on the grid as Closed (gray), including overnight. A room blackout is Closed too, not Unavailable — Unavailable is only another booking inside open hours.
